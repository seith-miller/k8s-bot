# Database connectivity
psycopg2-binary==2.9.9

# Environment variables
python-dotenv==1.0.0

# YAML parsing (if we want to validate manifests)
PyYAML==6.0.1

# Optional: Enhanced logging and formatting
colorlog==6.8.0

# Optional: Progress bars for long operations
tqdm==4.66.1

# Optional: JSON schema validation for k8s manifests
jsonschema==4.20.0

# Optional: Kubernetes Python client (if we want to use Python API instead of kubectl)
kubernetes==28.1.0
